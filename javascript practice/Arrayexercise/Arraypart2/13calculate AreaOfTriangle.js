// write a function to calculte area of a triangle
function calculateAreaOfTriangle(base, height) {
  return 0.5 * base * height;
}

const base = 6;
const height = 8;
const area =calculateAreaOfTriangle (base,height);
console.log(area); 
